 while (!interrupted)
    {
        handlesignal(SIGINT, &interrupted);
        handlesignal(SIGTSTP, &interrupted);

        if (interrupted)
        {
            printf("\n");
            interrupted = false;
            continue;
        } why is this neccessary
